# Envelope segments

This module contains definitions for envelope segments.

An envelope segment can be of dour types.

## Bezier segment

A bezier segment consists of a starting point (mapped to 0 in its  first dimension), a control point and an end point (mapped to 1 in its first dimension).


