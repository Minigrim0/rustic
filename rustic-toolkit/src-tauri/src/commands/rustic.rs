//! Rustic-related commands (app behaviour, settings, ...)
//!
//! Note: `change_render_mode` has been removed — the audio pipeline now uses a
//! single unified graph-based renderer with no explicit mode switching.
